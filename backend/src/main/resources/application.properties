spring.datasource.url=jdbc:postgresql://localhost:5432/saludfinanciera
spring.datasource.username=sergiogutierrez
spring.datasource.password=12345Guti
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Configuración de IA Local (Ollama) ---
# La URL por defecto donde corre Ollama en tu Mac
spring.ai.ollama.base-url=http://localhost:11434

# El modelo específico que estamos descargando
spring.ai.ollama.chat.options.model=llama3.2:3b

# Temperatura (0.7 es un buen equilibrio entre creatividad y precisión)
spring.ai.ollama.chat.options.temperature=0.7

spring.jackson.deserialization.fail-on-unknown-properties=false